name: Continuous integration workflow
on:
  pull_request:
    branches:
      - "main"
jobs:
  backend_ci:
    name: Backend CI
    uses: ./.github/workflows/backend-ci.yml

  backend_black:
    name: Backend Black CI
    uses: ./.github/workflows/backend-black.yml

  frontend_lint:
    name: Frontend CI
    uses: ./.github/workflows/frontend-ci.yml
